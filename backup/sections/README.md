# Backups de seções

- `Dores.v1.tsx` / `Dores.v1.css` — versão da seção "Quando todo mundo sabe o que fazer" antes dos cards que viram (dor → solução).
  Para voltar: copie os dois arquivos (renomeando para Dores.tsx / Dores.css) por cima de `src/sections/Dores.tsx` e `src/sections/Dores.css`.

Esta pasta não é importada pelo app.
