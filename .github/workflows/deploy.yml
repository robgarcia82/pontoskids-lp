name: Deploy no GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
        env:
          PAGES_BASE: /pontoskids-lp/
      # Nomes de arquivo são estáveis (sem hash); a query ?v=<commit> garante que um HTML novo
      # nunca receba CSS/JS antigos do cache do GitHub Pages (10 min).
      - run: |
          V=${GITHUB_SHA::8}
          sed -i "s#assets/app.js\"#assets/app.js?v=$V\"#; s#assets/index.css\"#assets/index.css?v=$V\"#" dist/index.html
          grep -o 'assets/[a-z]*\.[a-z]*?v=[0-9a-f]*' dist/index.html
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
